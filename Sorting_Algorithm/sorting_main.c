#include "sorting.h"
int main(SORT_UINT8_t av, char *argv[])
{
    // if(av > 1)
    // {
        // Buble_sort(argv)
    // }
    int tab[10] = {1,2,3,4,5,7,8,9,0,5};
    int size = sizeof(tab) / sizeof(*(tab));
   (tab, 0,10);
    for(int i = 0; i < size; i++)
    {
        printf("%d", tab[i]);
    }
    printf("\n%d", count);

}